'use strict';
//没完成

// function iseven(elem, index, arr) {
//   return index % 2 !== 0;
// }
//
// function shown(elem, index, arr) {
//   arr.find()
//   return index === arr.indexOf(elem);
// }

var single_element = function (collection) {
  // return collection.filter(iseven).filter(shown)
};
module.exports = single_element;
