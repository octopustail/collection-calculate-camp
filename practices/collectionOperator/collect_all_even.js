'use strict';

function collect_all_even(collection) {
  var collection_b = [];
  for(var i = 0; i<conllection.length; i++){
    if(collection[i] % 2 == 0){
      collection_b.push()
    }
  }
  return collection_b//在这里写入代码
}

module.exports = collect_all_even;
